<h1>Edit Event</h1>

<p><%= link_to "All Events", events_url %></p>
<p><%= link_to "Delete", event_path(@event), method: :delete, data: {confirm: "Are you sure you want to delete the event '#{@event.name}'? This cannot be undone!"} %></p>
<p><%= render "event_form", event: @event %></p>
